pipeline {
    agent any

    stages {
        stage('Docker build night') {
            steps {
                sh '''
                    docker build -f ci/Dockerfile -t my-playwright .
                '''
            }
        }
    }
}