pipeline {
    agent any

    stages {
        stage('Docker build night') {
            steps {
                sh '''
                    docker build -t my-playwright .
                '''
            }
        }
    }
}